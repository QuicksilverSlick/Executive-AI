---
import Button from './Button.astro';
import Icon from './Icon.astro';
---

<section class="section-padding bg-gradient-to-br from-brand-pearl to-gray-50 dark:from-dark-base dark:to-dark-surface relative overflow-hidden">
  <!-- Background decoration -->
  <div class="absolute inset-0 pointer-events-none">
    <div class="absolute top-0 right-0 w-96 h-96 bg-brand-navy/5 rounded-full blur-3xl"></div>
    <div class="absolute bottom-0 left-0 w-96 h-96 bg-brand-gold/5 rounded-full blur-3xl"></div>
  </div>
  
  <div class="container-narrow relative z-10">
    <div class="max-w-4xl mx-auto text-center">
      <div id="hero-content">
        <h1 class="text-4xl md:text-6xl font-bold text-brand-charcoal dark:text-dark-text mb-8 leading-tight">
          <span class="hero-line-1 opacity-0 block pb-2">Stop Falling Behind.</span>
          <span class="block text-gradient hero-line-2 opacity-0 mt-2 pb-2">Start Dominating.</span>
        </h1>
        <p class="text-xl md:text-2xl text-gray-700 dark:text-dark-text-secondary mb-8 hero-description opacity-0 px-4 sm:px-0">
          Our Private, Customized One on One AI Masterclass gives leaders the decisive competitive edge. 
          In weeks, you'll move from AI confusion to complete confidence.
        </p>
        <div class="flex flex-col md:flex-row gap-4 justify-center hero-buttons opacity-0 px-4 sm:px-0">
          <Button 
            href="#schedule" 
            size="lg"
            icon="lucide:calendar"
            class="calendly-trigger transform hover:scale-105 transition-all duration-300 w-full md:w-auto text-center justify-center whitespace-normal md:whitespace-nowrap md:min-w-[280px]"
          >
            <span class="hidden sm:inline">Schedule Your Custom Strategy Session</span>
            <span class="sm:hidden">Schedule Strategy Session</span>
          </Button>
          <Button 
            href="#audit" 
            variant="outline" 
            size="lg"
            icon="lucide:clipboard-check"
            class="transform hover:scale-105 transition-all duration-300 w-full md:w-auto text-center justify-center whitespace-normal md:whitespace-nowrap md:min-w-[280px]"
          >
            <span class="hidden sm:inline">Take the 2-minute AI Opportunity Audit</span>
            <span class="sm:hidden">Take AI Audit</span>
          </Button>
        </div>
      </div>
      
      <!-- Trust indicators -->
      <div class="mt-12 flex flex-col sm:flex-row items-center justify-center gap-6 sm:gap-8 text-sm text-gray-600 dark:text-dark-text-tertiary hero-trust opacity-0">
        <div class="flex items-center gap-3">
          <Icon name="lucide:lock" size={24} class="text-brand-gold dark:text-accent-gold flex-shrink-0" />
          <span class="text-center sm:text-left">100% Private & Confidential</span>
        </div>
        <div class="flex items-center gap-3">
          <Icon name="lucide:shield-check" size={24} class="text-brand-gold dark:text-accent-gold flex-shrink-0" />
          <span class="text-center sm:text-left">Money-Back Guarantee</span>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  @keyframes fadeInUp {
    from {
      opacity: 0;
      transform: translateY(30px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }
  
  .hero-line-1 {
    animation: fadeInUp 0.8s ease-out forwards;
  }
  
  .hero-line-2 {
    animation: fadeInUp 0.8s ease-out 0.2s forwards;
  }
  
  .hero-description {
    animation: fadeInUp 0.8s ease-out 0.4s forwards;
  }
  
  .hero-buttons {
    animation: fadeInUp 0.8s ease-out 0.6s forwards;
  }
  
  .hero-trust {
    animation: fadeInUp 0.8s ease-out 0.8s forwards;
  }
</style>