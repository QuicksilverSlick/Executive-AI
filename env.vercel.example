# Vercel Environment Variables Configuration
# Copy this to your Vercel project dashboard under Settings > Environment Variables

# === OpenAI Configuration ===
# Required: Your OpenAI API key for voice agent functionality
OPENAI_API_KEY=sk-your-openai-api-key-here

# === Voice Agent Security Configuration ===
# CORS origins - update with your actual domain
ALLOWED_ORIGINS=https://executiveaitraining.com,https://executiveaitraining.vercel.app

# Rate limiting for voice API endpoints (requests per minute)
VOICE_AGENT_RATE_LIMIT=100

# Session token duration in minutes
VOICE_AGENT_TOKEN_DURATION=60

# Maximum WebSocket connections per IP
MAX_WEBSOCKET_CONNECTIONS=5

# Voice session timeout in seconds
VOICE_SESSION_TIMEOUT=300

# === Site Configuration ===
# Your production domain
PUBLIC_SITE_URL=https://executiveaitraining.com

# Site name for metadata
PUBLIC_SITE_NAME="Executive AI Training"

# === Vercel-specific Configuration ===
# Deployment environment
VERCEL_ENV=production

# Region preference for edge functions
VERCEL_REGION=iad1

# === Optional Analytics Configuration ===
# Google Analytics (client-safe with PUBLIC_ prefix)
PUBLIC_GA_MEASUREMENT_ID=G-XXXXXXXXXX

# Vercel Analytics (automatically enabled in vercel.json)
VERCEL_ANALYTICS_ID=your-analytics-id

# === Optional Integration Configuration ===
# Calendly integration
PUBLIC_CALENDLY_URL=https://calendly.com/your-username

# Anthropic API Key (for Claude integration)
ANTHROPIC_API_KEY=sk-ant-your-anthropic-key-here

# === Performance and Monitoring ===
# Enable debug logging (development/staging only)
DEBUG_VOICE_AGENT=false

# OpenAI request timeout in milliseconds
OPENAI_REQUEST_TIMEOUT=30000

# Maximum audio file size in bytes (5MB default)
MAX_AUDIO_FILE_SIZE=5242880

# === Security Headers ===
# Content Security Policy
CSP_CONNECT_SRC="'self' https://api.openai.com wss://api.openai.com"

# === Development Configuration ===
# Set to 'development' for staging environments
NODE_ENV=production

# === Instructions for Vercel Dashboard ===
# 1. Go to your Vercel project dashboard
# 2. Navigate to Settings > Environment Variables
# 3. Add each variable above with appropriate values
# 4. Set appropriate environments (Production, Preview, Development)
# 5. For sensitive keys like OPENAI_API_KEY, use only Production environment
# 6. For PUBLIC_ prefixed variables, they can be used in all environments

# === Security Notes ===
# - Never commit actual API keys to version control
# - Use Vercel's encrypted environment variables
# - Regularly rotate API keys
# - Monitor API usage through Vercel Analytics
# - Set appropriate CORS origins for production
# - Use different API keys for staging/production environments